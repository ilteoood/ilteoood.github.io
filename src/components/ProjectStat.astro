---
interface Props {
  value: number;
  icon: string[];
}

const { value, icon } = Astro.props;
---

<span class="flex items-center gap-1">
  <svg class="w-4 h-4 fill-current" viewBox="0 0 512 512" set:html={icon} />
  {value}
</span>
